{
  "name": "matchday-copilot",
  "version": "1.0.0",
  "private": true,
  "description": "GenAI stadium-ops assistant for FIFA World Cup 2026 — Fan, Volunteer, Organizer, and Venue Staff modes in one chat app.",
  "workspaces": ["server", "client"],
  "scripts": {
    "install:all": "npm install --workspace=server && npm install --workspace=client",
    "test": "npm run test --workspace=server",
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client"
  }
}
